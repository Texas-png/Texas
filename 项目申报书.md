# MoonTodo · 轻量级待办事项管理系统 — 项目申报书

## 基本信息

- **项目名称：** MoonTodo · 轻量级待办事项管理系统
- **项目方向：** Moonbit Web 应用 / 效率工具
- **是否为移植项目：** 否

## 项目简介

MoonTodo 是一款基于 MoonBit 语言构建的轻量级待办事项管理应用，为用户提供简洁高效的待办任务管理体验。项目后端核心逻辑使用 MoonBit 编写，前端采用原生 HTML/CSS/JavaScript 响应式设计，涵盖任务的增删改查、状态管理、优先级分类等完整功能。系统面向个人用户和小型团队，提供从任务创建、跟踪到完成的一站式解决方案。

## 核心功能范围

- **任务数据模型**：基于 MoonBit 实现 Todo 的结构化定义，支持任务标题、描述、优先级（高/中/低）及状态（待办/已完成）
- **任务管理引擎**：使用 MoonBit 实现任务的核心业务逻辑，包括创建、查询、切换状态、删除及列表展示
- **CSV 数据持久化**：基于 CSV 文件实现数据的本地存储与读取，支持服务重启后数据恢复
- **RESTful API 服务**：提供基于 HTTP 的 JSON API，支持前端与后端的数据交互
- **前端交互界面**：采用响应式 Web 界面，包含任务列表展示、创建表单、状态切换、删除操作等完整交互流程
- **数据校验模块**：对用户输入进行合法性校验，确保数据完整性

## 技术方案

- **后端语言：** MoonBit
- **后端框架：** MoonBit HTTP Server（原生 HTTP 库）
- **前端技术：** 原生 HTML5 + CSS3 + JavaScript（ES6）
- **数据存储：** CSV 文件
- **API 风格：** RESTful JSON API
- **构建工具：** MoonBit 原生构建系统

## 项目结构

```
MoonTodo/
├── cmd/main/              # 后端入口
│   ├── main.mbt           # HTTP 服务器
│   └── moon.pkg.json      # 包配置
├── lib/                   # 后端核心库
│   ├── todo.mbt           # 数据模型
│   ├── store.mbt          # 存储引擎
│   ├── handler.mbt        # 请求处理
│   ├── validators.mbt     # 输入校验
│   └── config.mbt         # 配置管理
├── frontend/              # 前端页面
│   ├── index.html         # 主页面
│   ├── style.css          # 样式表
│   └── app.js             # 应用逻辑
├── static/data/           # 数据目录
│   └── todos.csv          # 待办数据
├── docs/                  # 文档
│   ├── design.md          # 设计文档
│   └── api.md             # API 文档
├── README.md              # 项目说明
└── moon.mod.json          # 模块配置
```

## API 设计

| 方法 | 路径 | 说明 | 请求体 |
|------|------|------|--------|
| GET | `/todos` | 获取全部待办 | - |
| POST | `/todos` | 创建待办 | `{title, description, priority}` |
| PUT | `/todos/:id` | 切换完成状态 | - |
| DELETE | `/todos/:id` | 删除待办 | - |
| GET | `/` | 前端页面 | - |

## 数据模型

```
Todo {
  id: Int           // 唯一标识
  title: String     // 任务标题
  description: String // 任务描述
  priority: Enum    // 优先级: Low | Medium | High
  status: Enum      // 状态: Pending | Completed
  created_at: String // 创建时间
}
```

## 预期成果

- 一个完整可运行的 MoonBit Web 应用
- 优雅的响应式前端界面
- 可靠的数据持久化方案
- 清晰的项目文档结构
- 开放的 API 接口设计
